## acceptedEULAVersion is private to a user
#if ($ctx.source.userId != $ctx.identity.cognitoIdentityId)
  #return ()
#end

{
  "version": "2018-05-29",
  "payload": $util.toJson($ctx.source.acceptedEULAVersion)
}
